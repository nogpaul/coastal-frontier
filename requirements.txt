# Direct dependencies only — transitive deps installed automatically
# Versions pinned for reproducibility

# Environment & secrets
python-dotenv==1.0.1

# HTTP client (for ACLED OAuth — kept for when API access is granted)
requests==2.32.3

# Tabular data
pandas==2.2.3
pyarrow==18.1.0

# Notebooks and visualization
jupyterlab==4.3.4
matplotlib==3.10.0
seaborn==0.13.2
